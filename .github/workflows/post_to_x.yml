name: Post to X

on:
  repository_dispatch:
    types: [run-post]

jobs:
  post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browsers
        run: npx playwright install chromium

      - name: Install Xvfb (for GUI mode)
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb
          echo "✅ Xvfb installed"

      - name: Run Playwright Post (with Xvfb)
        run: |
          export DISPLAY=:99
          echo "🖥️ DISPLAY環境変数を設定: $DISPLAY"
          
          # Xvfbを起動
          Xvfb :99 -screen 0 1280x800x24 > /tmp/xvfb.log 2>&1 &
          XVFB_PID=$!
          echo "🖥️ Xvfbを起動しました (PID: $XVFB_PID)"
          
          # Xvfbが起動するまで待機
          sleep 3
          
          # Xvfbが起動しているか確認
          if ps -p $XVFB_PID > /dev/null; then
            echo "✅ Xvfbは正常に起動しています"
          else
            echo "❌ Xvfbの起動に失敗しました"
            cat /tmp/xvfb.log
            exit 1
          fi
          
          # DISPLAY環境変数が設定されているか確認
          echo "🖥️ DISPLAY環境変数: $DISPLAY"
          
          # xdpyinfoでXサーバーが動作しているか確認
          xdpyinfo -display $DISPLAY > /dev/null 2>&1 && echo "✅ Xサーバーは正常に動作しています" || echo "⚠️ Xサーバーの確認に失敗しました"
          
          # npm run postを実行
          npm run post
          
          # クリーンアップ
          kill $XVFB_PID 2>/dev/null || true
        env:
          DISPLAY: :99
          TEXT: ${{ github.event.client_payload.text }}
          IMAGE: ${{ github.event.client_payload.file_id }}
          SLACK_WEBHOOK_URL: ${{ github.event.client_payload.slack }}
          DRIVE_FOLDER_ID: ${{ github.event.client_payload.drive_folder_id }}
          X_EMAIL: ${{ github.event.client_payload.X_EMAIL }}
          X_USERNAME: ${{ github.event.client_payload.X_USERNAME }}
          X_PASSWORD: ${{ github.event.client_payload.X_PASSWORD }}
          CI: 'true'